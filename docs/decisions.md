# Decision log

| ID | Date | Decision | Reason | Status |
|---|---|---|---|---|
| D-001 | 2026-08-18 | Focus GridSight on Energy Analytics, BI, and forecasting. | It complements the existing analytics/data-quality and backend projects. | Accepted |
| D-002 | 2026-08-18 | Use Germany for load/generation and DE/LU for day-ahead price. | This is coherent with the selected official source and target jobs. | Accepted |
| D-003 | 2026-08-18 | Use 2022-2025 at an hourly analytical grain. | Four complete years are meaningful but manageable and support train/validation/test separation. | Accepted |
| D-004 | 2026-08-18 | Use Bundesnetzagentur SMARD CSV snapshots as the essential data source. | It is authoritative, accessible, validated, and licensed under CC BY 4.0. | Accepted |
| D-005 | 2026-08-18 | Preserve raw snapshots and maintain a source manifest. | SMARD values can be revised; reproducibility requires immutable inputs and checksums. | Accepted |
| D-006 | 2026-08-18 | Use UTC as the canonical timestamp and retain Europe/Berlin reporting attributes. | This prevents ambiguous or missing keys at daylight-saving transitions. | Accepted |
| D-007 | 2026-08-18 | Forecast only Germany-wide hourly load for the next 24 hours. | It is relevant to target roles and avoids an inflated multi-target ML scope. | Accepted |
| D-008 | 2026-08-18 | Compare daily/weekly baselines, Ridge, and HistGradientBoostingRegressor. | This teaches interpretable and nonlinear modeling without adding another ML framework. | Accepted |
| D-009 | 2026-08-18 | Use PostgreSQL schemas `staging`, `analytics`, and `reporting`. | The separation is sufficient for lineage and BI without enterprise-style overengineering. | Accepted |
| D-010 | 2026-08-18 | Use a Power BI star model and a separate Excel monthly analyst pack. | The deliverables demonstrate distinct BI and business-user workflows rather than duplicate dashboards. | Accepted |
| D-011 | 2026-08-18 | Use Docker Compose only for PostgreSQL if available. | It improves reproducibility without making Docker part of GridSight's main portfolio story. | Accepted |
| D-012 | 2026-08-18 | Exclude Streamlit, backend APIs, streaming, cloud deployment, and deep learning. | These technologies duplicate existing projects or add scope without improving the target-job evidence. | Accepted |
| D-013 | 2026-08-18 | Add weather only after the load-only baseline is complete. | Weather adds sourcing and leakage complexity and must prove measurable value. | Accepted |
| D-014 | 2026-08-19 | Standardize local development on Python 3.13 with a project-local virtual environment and `pyproject.toml` dependency groups. | Python 3.13 is installed locally and supported by the selected analytics, forecasting, database, test, and lint packages; an isolated environment makes the setup reproducible. | Accepted |
| D-015 | 2026-08-19 | Run PostgreSQL 17.10 from the official Alpine Docker image and separate its integration test from the default fast test suite. | PostgreSQL 17 is supported through 2029, Compose makes the local runtime reproducible, and an explicit integration marker keeps normal development checks usable when Docker is stopped. | Accepted |
| D-016 | 2026-08-19 | Define six approved SMARD exports before acquisition and register raw snapshots through an immutable SHA-256 manifest workflow. | Fixed filters prevent scope drift, tracked lineage makes source claims auditable, and checksum-based overwrite protection preserves reproducibility when SMARD values are revised. | Accepted |
| D-017 | 2026-08-19 | Use the SMARD column `grid load [MWh] Calculated resolutions` as the load measure and treat local timestamp text as non-unique input. | The first real export distinguishes grid load from load including pumped storage, skips the spring 02:00 hour, and repeats the autumn 02:00 label; explicit column selection and UTC conversion prevent semantic and key errors. | Accepted |
| D-018 | 2026-08-19 | Parse SMARD `-` generation markers as missing/unavailable and never silently convert them to measured zero. | The 2024-2025 generation snapshot contains 16,836 dash markers in Nuclear after 708 explicit numeric zeroes, so the source itself distinguishes the two states. | Accepted |
| D-019 | 2026-08-19 | Select the exact `Germany/Luxembourg [€/MWh] Calculated resolutions` price column and retain negative values as valid market observations. | The SMARD export contains 17 market columns, while the approved target has 370 negative hours and an observed minimum of -500 EUR/MWh; selection by position or non-negative validation would corrupt the analysis. | Accepted |
| D-020 | 2026-08-19 | Preserve SMARD start/end text, disambiguate ordered Europe/Berlin starts, and derive every canonical interval end as UTC start plus one hour. | The spring source includes a nonexistent 02:00 end label, while both repeated autumn 02:00 starts show 03:00 as their end; independently localizing end text would create invalid or two-hour intervals. | Accepted |
| D-021 | 2026-08-19 | Keep the four actual-consumption measures in a canonical wide dataset, derive the forecast target `grid_load_mw` from hourly MWh, and retain negative residual load. | The source measures have distinct meanings, hourly MWh converts explicitly to average MW, and the real snapshots contain valid negative residual-load observations while the other consumption measures remain non-negative. | Accepted |
| D-022 | 2026-08-19 | Model clean generation as one row per UTC interval and technology, classify pumped-storage generation as storage, and represent the Nuclear `-` marker as unavailable with missing MWh/MW. | Long form supports technology dimensions and renewable aggregation, pumped storage is not itself renewable generation, and the source distinguishes 16,836 unavailable Nuclear values from 708 explicit zeroes. | Accepted |
| D-023 | 2026-08-19 | Keep only the exact Germany/Luxembourg series in the canonical price dataset and exclude the other bidding zones present in the SMARD export. | GridSight's approved geography is DE/LU; carrying unrelated market columns would expand scope and could introduce accidental feature leakage, while the immutable raw snapshot still preserves them for lineage. | Accepted |
| D-024 | 2026-08-19 | Validate all three clean frames before publication and emit deterministic issue and summary artifacts without a runtime timestamp. | A failed gate must preserve last known-good clean outputs, while stable check IDs, metrics, paths, and hashes make failures actionable and successful runs byte-reproducible. | Accepted |
| D-025 | 2026-08-19 | Mirror the three canonical datasets in `staging`, then model conformed date, hour, and technology dimensions with separate hourly electricity and interval/technology generation facts. | The two facts preserve their different grains, UTC remains the unambiguous fact key, local dimensions support BI, and combining aligned load and price avoids an unnecessary one-to-one fact relationship. | Accepted |
| D-026 | 2026-08-20 | Load only hash-matched passing Phase 3 artifacts, using client-side PostgreSQL `COPY` and an in-transaction full refresh followed by reconciliation. | The fixed four-year dataset is suitable for full replacement, `COPY` is efficient without server filesystem access, reruns cannot duplicate facts, and PostgreSQL rollback preserves the previous complete model if any step fails. | Accepted |
| D-027 | 2026-08-20 | Publish separate hourly energy, hourly technology, daily, and monthly reporting views; sum energy, average power and price, retain DST hour counts, and define renewable share against reported generation. | Stable grains isolate BI clients from internal tables, explicit unit-aware aggregation prevents invalid sums, observed-hour counts preserve 23/25-hour days, and the named denominator keeps the renewable KPI defensible when source generation is unavailable. | Accepted |
| D-028 | 2026-08-20 | Define descriptive KPIs in three read-only SQL grains and publish a deterministic checked JSON snapshot; sum energy, average power and price, preserve availability, and use reported generation as the renewable-share denominator. | Fixed definitions prevent notebooks and BI tools from drifting, exact grains and units keep comparisons valid, missing generation must not become zero, and byte-reproducible aggregate evidence is reviewable without committing row-level data. | Accepted |
| D-029 | 2026-08-20 | Limit exploratory analysis to monthly movement, weekday/weekend local-hour load shape, daily Pearson associations overall and by year, and six transparent unusual-day rankings; compare daily load extrema using average GW. | Fixed questions prevent notebook sprawl, separate years expose changing price regimes, explicit rankings are interview-defensible, and average power avoids mechanically favoring 23/25-hour DST dates while preserving observed-hour context. | Accepted |
| D-030 | 2026-08-20 | Issue one load forecast at each Europe/Berlin local midnight for the next 24 real hourly intervals; train on 2022-2023 after seven history days, validate on 2024, reserve 2025 for one final test, and permit only information complete by each origin. | Local midnight is a clear recurring business schedule, canonical UTC keeps DST horizons unambiguous, 168 history hours support both seasonal baselines, chronological roles prevent future leakage, and an untouched recent year provides honest final evidence. | Accepted |
| D-031 | 2026-08-20 | Evaluate 24-hour and 168-hour seasonal-naive load baselines on training and validation origins only; publish aggregate overall and horizon metrics while rejecting any scored 2025 test row. | Transparent lag baselines define the minimum model benchmark, exact recorded source timestamps make availability auditable across DST, aggregate evidence avoids committing target-level predictions, and preserving the test boundary prevents premature design feedback. | Accepted |
| D-032 | 2026-08-20 | Build 27 deterministic load-only features from known target calendars and observations complete by each origin; persist train/validation labels but redact every 2025 test label in the model matrix. | Calendar, lag, and rolling signals support both linear and tree models without uncertain exogenous availability, exact UTC windows prevent DST leakage, explicit feature columns are auditable, and redaction makes premature test scoring mechanically harder. | Accepted |
| D-033 | 2026-08-20 | Compare three train-scaled Ridge alphas and two fixed histogram gradient-boosting capacities; disable internal early stopping, select by overall 2024 validation MAE, and require comparison with the frozen weekly baseline before unlocking test targets. | A small predeclared search is interview-defensible, covers linear and nonlinear behavior, avoids random internal holdouts and excessive validation tuning, and preserves 2025 as a one-time final estimate. | Accepted |
| D-034 | 2026-08-20 | Freeze the Step 6.4 winner, refit that unchanged design on combined 2022-2024 development rows, unlock and score 2025 once, compare with both seasonal-naive baselines, and prohibit further model selection. | Combining development rows uses all pre-test history after selection is complete, exact hash and alignment gates preserve lineage, a single untouched recent-year evaluation supports an honest generalization claim, and forbidding post-test tuning preserves that claim. | Accepted |
| D-035 | 2026-08-26 | Freeze eight BI/Excel products before implementation: four existing energy SQL views, two planned forecast-performance SQL views, and checked data-quality and source-lineage extracts; require explicit grains, keys, units, consumers, and fixed sample policies. | A predeclared semantic interface prevents Power BI, Excel, SQL, and Python from drifting, keeps UTC/DST and unit rules auditable, limits public extracts to compact deterministic evidence, and gives later database and export work exact acceptance targets. | Accepted |
| D-036 | 2026-08-26 | Load the final 2025 predictions into a constrained, hash-lineaged forecast-evaluation fact and publish separate hourly and 75-row metric views; full-refresh forecast rows only after the canonical energy facts. | BI tools need durable SQL products rather than an ignored CSV, while exact artifact hashes, conformed time keys, transactional reconciliation, and a fixed refresh order preserve the frozen final-test evidence and prevent metric drift. | Accepted |
| D-037 | 2026-08-28 | Publish only eight fixed, deterministic reporting samples through a repeatable-read SQL snapshot and checked source transforms; validate exact scopes, schemas, keys, counts, hashes, attribution, and destination permission inheritance, then publish a manifest last. | Compact evidence makes the portfolio and later BI files reviewable without committing full datasets, while fixed filters, source hashes, destination-local atomic replacement, and manifest-last publication prevent inaccessible, partial, or drifting extracts from appearing verified. | Accepted |

## How to update this log

Add a new row when a choice changes the project's scope, data contract, architecture, forecasting contract, KPI meaning, or final deliverables. Do not silently rewrite an accepted decision; add a superseding decision and reference the earlier ID.
