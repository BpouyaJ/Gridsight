# Generated analytical results

This directory contains small, reviewable analytical outputs derived from the
verified PostgreSQL reporting layer. Full row-level datasets remain ignored
under `data/processed/`.

`kpi_snapshot.json` is generated by:

```powershell
python -m gridsight.reporting.build_kpis
```

The snapshot is deterministic for unchanged reporting data and query code. It
contains aggregate public-data results, declared units, source attribution,
and query-contract metadata suitable for Git and portfolio review.

The verified Step 5.1 artifact has SHA-256
`fa03ee1af919027634aeb45a524c713274bd9effcac9955052d3be449c8395fc`.

`eda_snapshot.json` and the four files under `reports/figures/` are generated
by:

```powershell
python -m gridsight.reporting.build_eda
```

The EDA builder verifies the KPI dependency and all monthly, local-hour, and
daily grains before publishing descriptive results.

The verified Step 5.2 artifacts have these SHA-256 values:

- `eda_snapshot.json`:
  `8e2aed814ed1178dd6107974761d571dbae512bf7b6943fd72dee81b4ca7944d`;
- `01_annual_kpis.png`:
  `faca9bc2cd26474d2bafefb39ae0037ab4e5334fc4f8d5e49bd09fc82b515118`;
- `02_monthly_energy_market.png`:
  `5b6889398b1bd01a9a37caf911264d923be336608376f4c62fd3cee5c34d7411`;
- `03_weekday_weekend_load_shape.png`:
  `c4dd6cd2e0e1c997cc859be740b5b90021076cffcd73a36193d9312b292c909b`;
- `04_renewable_share_vs_price.png`:
  `e7c9ac69b8ccd56ad4c90054152f6800845e7a61d21d7d84be7d7ebc6e217af9`.

`forecast_contract.json` is generated by:

```powershell
python -m gridsight.forecasting.build_contract
```

It records the deterministic Step 6.1 schedule, information cutoff,
chronological splits, target, metric definitions, row counts, and ignored
forecast-index hash.

The verified Step 6.1 artifacts have these SHA-256 values:

- `forecast_contract.json`:
  `3a4a2da987089258b3ba5e7d64bc67f0abd62a0a4c5e3fdd31c03df75a060f88`;
- ignored `data/processed/forecast_index.csv`:
  `6089b1d7c2cd3298cfa3d24526f98cc763b1c95947ce2e9e84ff06dc42bbecc4`.

`baseline_snapshot.json` is generated by:

```powershell
python -m gridsight.forecasting.build_baselines
```

It contains only aggregate Step 6.2 train and validation metrics, their
24-step horizon profiles, the stronger validation baseline, frozen-input
hashes, and an explicit zero-test-row record.

The verified `baseline_snapshot.json` SHA-256 is
`311513a0405761aa6a30db6a956b53c29b3cc38dfd03dc4e74efa62902a4b717`.

`feature_contract.json` is generated with the ignored row-level feature matrix
by:

```powershell
python -m gridsight.forecasting.build_features
```

It records Step 6.3 feature names and families, split counts, redacted-test
rules, frozen-input hashes, and the ignored matrix hash.

The verified Step 6.3 artifacts have these SHA-256 values:

- `feature_contract.json`:
  `daac05d8a00a3db3eedb29671d4543e497607aac7e8431898a413effb4ad65ae`;
- ignored `data/processed/forecast_features.csv`:
  `eda6e21687fe3cd09681de14370749a03cbb974d81972661196c86b1a4d52ef8`.

`model_validation_snapshot.json` is generated by:

```powershell
python -m gridsight.forecasting.train_models
```

It records Step 6.4 fixed candidate parameters, train/validation metrics,
horizon profiles, selected model, weekly-baseline comparison, source hashes,
runtime version, and zero published test results.

The verified `model_validation_snapshot.json` SHA-256 is
`b6c2b96482e238249300612ee6750b278f63aabb970f56e4f2c150ec67d013f7`.

`final_evaluation_snapshot.json` is generated with the ignored row-level test
prediction artifact by:

```powershell
python -m gridsight.forecasting.evaluate_final
```

Step 6.5 hash-gates every frozen forecasting dependency, refits only the
already-selected design on train plus validation rows, and publishes the one
final 2025 comparison with both seasonal-naive baselines. The snapshot contains
aggregate overall and horizon metrics; the detailed 8,760-row predictions stay
ignored under `data/processed/`.

The verified Step 6.5 artifacts have these SHA-256 values:

- `final_evaluation_snapshot.json`:
  `d65eea94653b1367ec169de60d4ff91fe2a956fa317040746c5a0a3c56fd3065`;
- ignored `data/processed/final_forecast_predictions.csv`:
  `e6e1a5c64372942142993e81f8f3f748b609dda67a15a66af5e48260686b38e6`.

`reporting_mart_contract.json` is generated by:

```powershell
python -m gridsight.reporting.build_mart_contract
```

Step 7.1 verifies the reporting SQL, final forecast, clean-data gate, and source
manifest before freezing eight Power BI/Excel data products and their checked
sample-extract policies. Step 7.2 updates the same contract after implementing
the two forecast SQL marts. Step 7.3 marks all samples implemented and adds
`sample_extract_manifest.json` with the eight checked output hashes.

The Step 7.1 frozen-design SHA-256 was
`acb7d3137aaa2c7c4f6fd688520987e5c573cbbeba245938e564cbc5cf1abdbc`.
The Step 7.2 implementation SHA-256 is
`2b6e765eb106a706ae87b1e6c22d502b8bc18e3d995bf429a4d5889051532f95`.

The checked sample bundle is generated by:

```powershell
python -m gridsight.reporting.build_samples
```

The command requires all 28 live reporting reconciliations, publishes eight
fixed CSVs under `data/samples/`, and writes
`reports/sample_extract_manifest.json`. The verified Step 7.3 reporting-mart
contract SHA-256 is
`54a55962b79d14508eb50882578f2277b9201a678a055006c06f383632c71110`;
the manifest SHA-256 is
`b0a377003820b1321b6b55fb290f08ae07eff2f8e06e4688c9788b42ec42f150`.

`powerbi_semantic_model_contract.json` is generated by:

```powershell
python -m gridsight.reporting.build_powerbi_contract
```

The Step 8.1 contract hash-gates the implemented reporting contract, checked
sample manifest, KPI snapshot, and final forecast evaluation. It freezes 15
Import tables, 12 active one-to-many single-direction relationships, 30 DAX
measures, five report pages, and exact Power BI reconciliation values. The
companion DAX catalogue is written to `powerbi/dax/measures.dax`.
The verified semantic-model contract SHA-256 is
`6f94e6bae7d426fd0a1014cf9665d02c857679e36fd773be22e23ab51d0fb32e`;
the DAX catalogue SHA-256 is
`de63d5a9964a08968674bb56bf7eea0a3bb316d937eaef6d9cc8e92d38e676df`.
